# event-ingestion-worker

Demo worker that consumes queues and writes graph-ready facts.

## Architecture Notes

- owner_team: LCM Platform
- service: event-ingestion-worker
- environment: demo
- database: postgres
- queue: nats.events.demo

This repository is intentionally public demo data for Company Brain webhook, graph, timeline, and retrieval testing.
